# Initialization of Backend
terraform init

# Made format of `.tf` file
terraform fmt

# Made validation (syntax) of `.tf` file
terraform validate

# Preview of what terraform will do
terraform plan

# Executes the actions proposed in a Terraform plan
terraform apply

# Show
terraform show

# Destroy
terraform destroy